'use client';

type PersonaPickerProps = {
  selectedId: string;
};

export default function PersonaPicker(_: PersonaPickerProps) {
  // Persona selection UI is intentionally disabled here
  return null;
}

